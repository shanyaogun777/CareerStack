# 复制为 .env 或 .env.local 后由 Vite 注入（仅构建时生效）。
# ⚠️ 所有 VITE_* 变量会打进前端 JavaScript，切勿在公开仓库提交含真实密钥的 .env。

# 可选：默认 DeepSeek / OpenAI 兼容 API Key（用户仍可在应用「设置」中覆盖）
# VITE_DEEPSEEK_API_KEY=

# 可选：默认 API Base（OpenAI 兼容）
# VITE_AI_BASE_URL=https://api.deepseek.com/v1

# 可选：默认模型名
# VITE_AI_MODEL=deepseek-chat
